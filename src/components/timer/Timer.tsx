type TProps = {
  /** */
};

const Timer = ({}: TProps) => {
  return (
    <div className="bg-white/20 w-[1000px] h-[300px] flex justify-center items-center ">
      타이머
    </div>
  );
};

export default Timer;
